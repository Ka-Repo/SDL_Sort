#pragma once

class Algorithms
{
private:
public:
	template <typename T> void mergesort(T first, T last, [](T first, T last));
	template <typename T> void bubblesort(T first, T last, [](T first, T last));
	template <typename T> void quicksort(T first, T last, [](T first, T last));
};
